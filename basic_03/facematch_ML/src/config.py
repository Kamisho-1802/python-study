"""共通設定。

学習メモ:
  モデルと検出器は「インデックス作成（フェーズ2）」と「検索（フェーズ3）」で
  必ず一致させること。別モデルで作ったベクトルは比較できないため、
  定数はこの1か所にまとめて全スクリプトから読み込む。
"""

# 埋め込みモデル（512次元）。途中で変えないこと。
MODEL = "Facenet512"

# 顔検出バックエンド。
DETECTOR = "retinaface"

# 入出力ディレクトリ（このファイルからの相対ではなく、プロジェクト直下基準で使う）
DATA_DIR = "data/celebs"
OUT_DIR = "index"
